apiVersion: v1
kind: Pod
metadata:
  name: smoke-test-client
  labels:
    app.kubernetes.io/name: smoke-test-client
spec:
  restartPolicy: Always
  containers:
    - name: client
      image: busybox:1.36
      command:
        - sh
        - -c
        - sleep 3600
