apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: required-owner-label
spec:
  policyName: required-owner-label
  validationActions:
    - Warn
    - Audit
  matchResources:
    namespaceSelector:
      matchLabels:
        kubernetes.io/metadata.name: w10-rbac-lab
